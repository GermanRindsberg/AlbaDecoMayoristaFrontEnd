import { BuscadorUsuarioPipe } from './buscador-usuario.pipe';

describe('BuscadorUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
